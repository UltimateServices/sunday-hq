import { StubRoute } from "@/components/shared/StubRoute";

export default function ModelPerformancePage() {
  return (
    <StubRoute
      title="Model Performance"
      phase={6}
      readiness="PENDING"
      lede="Calibration, CLV, error by market. Placeholder CDF in Phase 2 is not a model to grade yet."
    />
  );
}
