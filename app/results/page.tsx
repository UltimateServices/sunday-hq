import { StubRoute } from "@/components/shared/StubRoute";

export default function ResultsPage() {
  return (
    <StubRoute
      title="Results"
      phase={6}
      readiness="PENDING"
      lede="Settled tickets, closing-line value, and hit/miss. Record then learn. Empty on purpose until games grade."
    />
  );
}
